import React, { Component } from 'react'
import ActivityCard from "../ActivityCard";
import PropTypes from 'prop-types'

class ActivityCardContent extends Component{
  static propTypes = {
  }
}

export default ActivityCardContent
